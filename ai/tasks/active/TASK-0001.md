# TASK-0001: Improve theme contrast with blue accents and add dark mode toggle

## Context
The docs UI needs better readability through stronger contrast while introducing a blue-accent visual identity. The page should support dark mode with a top-right toggle, defaulting to system preference and allowing user override persistence. This task should focus on shared theme tokens and layout-level UI behavior so the change applies consistently across the site. The expected outcome is a balanced accessibility and visual polish improvement with minimal behavioral risk.

## References
- **Agent**: [code-generator](../agents/code-generator.agent.md)
- **Instructions**:
  - [architecture](../instructions/architecture.instructions.md)
  - [style](../instructions/style.instructions.md)
  - [testing](../instructions/testing.instructions.md)

## Dependencies
None

## Steps
1. [ ] Audit current theme/color usage in shared layout and styling system; identify token points for text/background contrast and accent color application.
2. [ ] Add/update shared design tokens for contrast-safe foreground/background pairs and blue accent roles (primary, hover, focus, subtle surfaces), then apply them to shared layout-level styles/components.
3. [ ] Implement a top-right dark mode toggle that defaults to system preference (`prefers-color-scheme`) and persists user override in local storage.
4. [ ] Ensure toggle state is applied at app shell/layout level to avoid per-page drift and confirm no SSR/client hydration issues.
5. [ ] Add or update unit tests for theme toggle logic (default selection, override persistence, and mode resolution behavior).
6. [ ] Document the finalized design tokens and usage guidance in `ai/instrcutions/architecture` (confirm exact path/name before merge).

## Acceptance Criteria
- [ ] Contrast improvements are visibly applied through shared theme tokens and meet the project’s accessibility target (balanced with design intent).
- [ ] Blue accents are consistently present in primary interactive and emphasis states without reducing readability.
- [ ] A top-right dark mode toggle exists, defaults to system preference, and correctly persists user override across reloads.
- [ ] Unit tests cover dark mode toggle logic and pass in CI/local test runs.
- [ ] Design tokens are documented in the requested architecture instruction location.

## Notes
- Confirm whether `ai/instrcutions/architecture` is intentional or a typo for `ai/instructions/architecture.instructions.md` before finalizing docs updates.
- Keep scope limited to theme/token + layout-level behavior; avoid unrelated refactors.